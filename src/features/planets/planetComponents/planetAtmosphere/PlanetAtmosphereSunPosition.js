const PlanetAtmosphereSunPosition = ({ children, className }) => {
  return (
    <div className={`${className} right-0 rounded-full z-30`}>{children}</div>
  );
};

export default PlanetAtmosphereSunPosition;
