const PlanetSurface = ({ surface }) => {
  /**
   * Filter array items based on search criteria (query)
   */

  return (
    <div
      className={`planet-background-bottom-third absolute h-29 top-64 w-full text-black z-1 overflow-hidden bg-brown}`}
    >
      planetSurface
    </div>
  );
};

export default PlanetSurface;
